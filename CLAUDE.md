# AROMA Project — Claude Rules

## 테스트 정책

- **새로운 테스트 코드를 작성하지 않는다.**
- **Python 빌드 테스트(`pytest` 등)를 실행하지 않는다.**
- 코드 수정 후 검증은 Colab 환경에서 직접 수행한다.
- 기존 `tests/` 디렉터리의 파일은 그대로 유지한다.

## 작업 방식

- 코드를 수정하면 Colab에서 실행할 수 있도록 변경 내용을 설명한다.
