# 작업일지 2026-06-11

## valley 과감지 수정 — bounded feature CV 기반 prominence 동적 조정

**파일**: `scripts/distribution_profiling.py`

**변경 내용**:
- 상수 추가: `BOUNDED_FEATURES`, `FLAT_CV_THRESHOLD=0.5`, `BOUNDED_VALLEY_PROMINENCE_RATIO=0.3`
- `_detect_valleys(values, feature_name=None)` — 시그니처 확장
- bounded feature(circularity, eccentricity, extent)에서 히스토그램 CV < 0.5(flat)이면 prominence ratio 0.1→0.3 상향
- CV 계산을 raw values 기준으로 수행 (log_transform 경로와 독립)
- 호출부: `_detect_valleys(values, feat)` 인자 추가
- 추가 수정: bounded feature만 Sturges bins 적용, 비bounded는 MAX_HISTOGRAM_BINS(50) 유지
  - 이유: Sturges bins을 전체 적용하면 비bounded feature(aspect_ratio, linearity) 회귀 발생
  - Colab 실행 결과 확인: 4/3/0 과잉 억제 → bins 분리로 수정
- 최종 결과: isp=13, cable=16, cashew=15 / anchor=19.2
  - circularity: 6/12/5 → 0/1/0 ✅
  - eccentricity: 3/7/11 → 1/0/0 ✅
  - linearity, solidity, aspect_ratio: 변화 없음 (회귀 없음) ✅
- phase0_analysis.md 섹션 2.1, 6.4, 6.5 수치 갱신 완료

**배경**: bins 수정(Sturges' rule) 이후에도 circularity(mvtec=12), eccentricity(cashew=11) 과감지 동일. VALLEY_PROMINENCE_RATIO=0.1이 flat distribution에서 낮아 잡음 peak 통과가 실제 원인.

---

## Phase 0 분석 — Data-Driven Anchor 설계 문서화

**파일**: `AROMA연구분석/phase0_analysis.md`

**추가 내용**: 섹션 6 "Data-Driven Anchor 설계" 신규 추가

### 주요 내용

- **6.1 Feature 분류**: Bounded(inv_silhouette, cluster_count) vs Unbounded(valley_count, entropy, FreqComplexity, OrientVariance) 분류표
- **6.2 Anchor 산출 공식**: n<100 → max×1.2, n≥100 → p95
- **6.3 anchor.json 스키마**: version, created_from, n_datasets, per-metric anchors 구조 제안
- **6.4 시뮬레이션 결과**: valley_counts=[27,39,37] → anchor=46.8 → norm 판별력 복원 (기존 모두 1.000 → isp 0.577 / cable 0.833 / cashew 0.791)
- **6.5 미해결 이슈**: bins fix 선행 조건, threshold_n=100 현실성, entropy 단위 미확인

---

## Step 1 — CCI sub-sampling + valley_count per-class mean 구현

**파일**: `scripts/aroma/compute_complexity.py`

**변경 내용**:

### CCI sub-sampling

- `compute_cci()`에서 `context_X_full` 전체를 사용하던 방식 → `min(n, max_patches=20000)` random sub-sampling 후 사용
- `_cluster_context()`는 이미 내부 sub-sampling 있었으나 `_col_mean`, `_col_var`는 전체 사용 중이었음 → 통일
- `complexity_report.json` CCI 컴포넌트에 `n_context_patches_used`, `n_context_patches_total` 진단 필드 추가
- 효과: 고해상도 데이터셋 패치 수 > 20k시 CCI 과대평가 방지

### valley_count per-class mean

- `_perclass_valley_count(morph_rows, aggregation="mean")` 함수 신규 추가
  - `defect_type`별 그룹핑 → 클래스별 feature별 valley count → 클래스 합산 → mean
  - scipy 없으면 0.0 fallback, `counts.max() == 0` 가드 추가
  - 빈 문자열 defect_type 처리: `r.get("defect_type") or "unknown"`
- `load_phase0_outputs()`에 `morph_rows_raw` 반환 추가
- `compute_mci()`에서 `_total_valley_count(dist_analysis)` → `_perclass_valley_count(morph_rows_raw)` 로 교체 (fallback 유지)
- 효과: cable(8종) vs isp(2종) 비교시 클래스 수 편향 제거

**재실행 필요**: 3개 데이터셋 Step 1 재실행 후 MCI 순서 (cable > cashew > isp) 확인 필요
