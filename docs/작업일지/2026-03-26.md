# 작업일지 — 2026-03-26

## 현재 파이프라인 진행 상태 (check_progress 결과)

```
ISP       ASM    : S2 미완(24/33)  S4 미완(21/26)  S5 미완(21/22)
ISP       LSM_1  : S4 미완(0/49)   S5,S6 미착수
ISP       LSM_2  : S3 미완(17/63)  S4 미완(0/17)   S5,S6 미착수
MVTec     wood   : S1 미착수 (이하 연쇄)
MVTec     zipper : S1 미착수 (이하 연쇄)
MVTec     bottle : S4 미완(3/20)   S5 미완(3/4)    S6 미착수
MVTec     나머지 13개: S4 미착수   S5,S6 미착수
VisA      candle : S4 미완(4/100)  S5 미완(0/4)    S6 미착수
VisA      나머지 11개: S4 미착수   S5,S6 미착수
```

> 모든 셀은 `docs/작업일지/stage1_6_execute.md` 의 코드를 사용한다.
> skip 로직 내장 → 완료된 항목 자동 건너뜀, 도메인 전체 셀 실행으로 OK.

---

## 실행 계획

### Phase 1 — Stage 1 보완 (MVTec wood, zipper)

**대상:** mvtec/wood, mvtec/zipper (2개)
**셀:** Stage 1 배치 실행 — `DOMAIN_FILTER = "mvtec"` (나머지 13개는 자동 skip)

```python
DOMAIN_FILTER = "mvtec"
```

완료 sentinel: `{cat_dir}/stage1_output/roi_metadata.json`

---

### Phase 2 — Stage 2 보완 (ISP ASM)

**대상:** isp/ASM (33개 중 9개 잔여)
**셀:** Stage 2 배치 실행 — `DOMAIN_FILTER = "isp"` (완료 24개 자동 skip)

```python
DOMAIN_FILTER = "isp"
```

완료 sentinel: `{cat_dir}/stage2_output/{seed_id}/` PNG ≥ NUM_VARIANTS

---

### Phase 3 — Stage 3 보완 (ISP LSM_2)

**대상:** isp/LSM_2 (63개 중 46개 잔여)
**셀:** Stage 3 배치 실행 — `DOMAIN_FILTER = "isp"` (완료 17개 자동 skip)

```python
DOMAIN_FILTER = "isp"
```

완료 sentinel: `{cat_dir}/stage3_output/{seed_id}/placement_map.json`

> Phase 2 완료 후 실행 (ASM의 새 seed들도 Stage 3 필요)

---

### Phase 4 — Stage 4 전체 (대부분 미착수)

대상 규모:

| 도메인 | 미완료 카테고리 | 미완료 seeds |
|--------|--------------|------------|
| ISP    | LSM_1(49), ASM 잔여, LSM_2 잔여 | ~60+ |
| MVTec  | 15개 전체 (bottle 17개 잔여 포함) | ~360+ |
| VisA   | 8개 전체 (candle 96개 잔여 포함) | ~796+ |

**순서:** isp → mvtec → visa (각각 별도 셀, skip 자동 처리)

```python
# 순서대로 실행 (DOMAIN_FILTER 바꿔서)
DOMAIN_FILTER = "isp"    # Phase 2,3 완료 후
DOMAIN_FILTER = "mvtec"  # Phase 1 완료 후
DOMAIN_FILTER = "visa"   # 언제든 실행 가능
```

완료 sentinel: `{cat_dir}/stage4_output/{seed_id}/defect/*.png`

---

### Phase 5 — Stage 5 전체 (Stage 4 완료 후)

```python
DOMAIN_FILTER = "isp"    # 실행
DOMAIN_FILTER = "mvtec"  # 실행
DOMAIN_FILTER = "visa"   # 실행
```

완료 sentinel: `{cat_dir}/stage4_output/{seed_id}/quality_scores.json`

---

### Phase 6 — Stage 6 전체 (Stage 5 완료 후)

```python
DOMAIN_FILTER = "isp"    # 실행
DOMAIN_FILTER = "mvtec"  # 실행
DOMAIN_FILTER = "visa"   # 실행
```

완료 sentinel: `{cat_dir}/augmented_dataset/build_report.json`

---

## 실행 체크리스트

```
[ ] Phase 1: Stage 1 mvtec     (wood, zipper)
[ ] Phase 2: Stage 2 isp       (ASM 9 seeds)
[ ] Phase 3: Stage 3 isp       (LSM_2 46 seeds)  ← Phase 2 완료 후
[ ] Phase 4a: Stage 4 isp      (~60+ seeds)       ← Phase 3 완료 후
[ ] Phase 4b: Stage 4 mvtec    (~360+ seeds)      ← Phase 1 완료 후
[ ] Phase 4c: Stage 4 visa     (~1196+ seeds)
[ ] Phase 5a: Stage 5 isp      ← Phase 4a 완료 후
[ ] Phase 5b: Stage 5 mvtec    ← Phase 4b 완료 후
[ ] Phase 5c: Stage 5 visa     ← Phase 4c 완료 후
[ ] Phase 6a: Stage 6 isp      ← Phase 5a 완료 후
[ ] Phase 6b: Stage 6 mvtec    ← Phase 5b 완료 후
[ ] Phase 6c: Stage 6 visa     ← Phase 5c 완료 후
```

> Phase 4b/c 와 Phase 4a 는 병렬 실행 가능 (Colab 세션 분리 또는 순차 실행).

---

## 유의 사항

**ISP wood/zipper Stage 1 완료 후 1b→2→3→4→5→6 연쇄 실행 필요**
→ Stage 1 배치 셀 이후 `DOMAIN_FILTER = "mvtec"` 로 Stage 1b~6 순차 실행.

**Stage 4 병렬 설정 (VisA 100 seeds/category):**
```python
SEED_THREADS = 2    # Drive 동시 쓰기 안정성
IMG_WORKERS  = -1   # 내부 CPU 최대 활용
```

**중간 점검:** Phase 4 시작 전/후 `check_progress` 셀 실행으로 진행 확인 권장.
