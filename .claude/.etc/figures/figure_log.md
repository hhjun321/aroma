# 그림 통합 실행 로그

SOP: `GRADUATION/SOP_figures_통합.md`

## 진행 현황

| 장 | STEP 1~4 (매핑·본문 작성) | STEP 5 (이미지 파일 배치) |
|----|--------------------------|--------------------------|
| 제3장 | 완료 (2026-09-16) | 완료 — 10개 파일 |
| 제4장 | 완료 (2026-09-16) — 그림 6개 | 완료 — 6개 파일 |
| 제5장 | 완료 (2026-09-16) — 그림 17개(원문 19장) | 완료 — 19개 파일 |
| 제6장 | 완료 (2026-09-16) — 그림 9개 | 완료 — 9개 파일 |

## 원문 불일치 (수정하지 않음 — 사용자 확인 대상)

### L-01. 제3장 표 3-1 합계 vs 본문 총량

- **위치**: 학위논문 표 3-1 / 3.2절 본문. 원문 표 1 / 3-5절.
- **내용**: 표의 "Images (증강 후)" 열 합계는 **826**(138+136+134+134+140+144)인데, 본문은 "총 **972**장, 학습 826 / 검증 146"으로 기술한다.
- **관찰**: 826이 표 합계와 학습 분할 수에 동시에 일치 → 표가 학습 분할만 집계했거나 972가 오기일 가능성.
- **조치**: 원문과 학위논문 모두 동일 수치를 쓰고 있어 그대로 유지. 확인 필요.

## 판정 대기 (사용자 결정 필요)

### D-01. 제3장 그림 3-9 패널 라벨 — **[해결]**

- 원문 그림 9는 두 이미지(`image3.jpg`, `image11.jpg`)가 하나의 캡션을 공유한다.
- STEP 5에서 이미지를 육안 확인: `image3.jpg`(fig3-9a)는 최종 약 1.0~1.6 구간 수렴 → **Train Loss**,
  `image11.jpg`(fig3-9b)는 최종 약 1.5~2.0 구간 수렴 → **Test Loss**. 본문 서술("Test가 전 구간에서 높게 유지")과 일치.
- 캡션을 `(a) Train Loss, (b) Test Loss`로 확정하였다.

### D-02. 제4장 미대응 원문 그림 4장 삽입 여부 — **[해결: 전량 삽입]**

원문 그림 1(결함/마스크 이미지), 2(생성된 결함/마스크), 3(유효 영역 추출), 4(Poisson blending).
→ 2026-09-16 사용자 승인으로 4장 모두 삽입. 성능 그래프는 그림 4-5·4-6으로 이동.

### D-03. 제5장 미대응 원문 그림 2장 삽입 여부 — **[해결: 전량 삽입]**

AROMA 원문 Figure 10(Background assignment on real images), Figure 11(Ring context).
→ 2026-09-16 사용자 승인으로 삽입(그림 5-8·5-9). 기존 5-8~5-15는 5-10~5-17로 이동.
   이에 수반해 5.2절에 배경 할당(bg_score)·자리 확정(site_score) 문단을 원문에서 새로 발췌·추가.

### D-04. 제6장 미대응 원문 그림 삽입 여부 — **[해결: 전량 삽입]**

CASDA 원문 Figure 1(Pipeline), Figure 4(대표 ROI 샘플), Figure 5~7(임계값 근거 분포).
→ 2026-09-16 사용자 승인으로 전량 삽입, 원문 9개 그림 모두 반영(제외는 CC 로고 1개).
   본문 등장 순서에 맞춰 번호를 재부여하여 기존 6-1~6-4는 6-2·6-4·6-8·6-9로 이동.
   이에 수반해 6.2절에 임계값 도출 근거(N=19,958 분포, 로그 스케일 Otsu) 서술을 새로 발췌·추가.

## 전 장 STEP 1~4 완료 (2026-09-16)

학위논문 전체 플레이스홀더 0건. 장별 그림 수: 제3장 9 / 제4장 6 / 제5장 17 / 제6장 9 = **41개**
(이미지 파일 기준 45개 — 3-9, 5-4, 5-5가 각 2패널).

### D-05. 제5장 그림 5-4·5-5 캡션 범위 조정 — **[해결: 반영]**

원문(Figure 4·5 / 6·7)은 Severstal·AITeX 2종만 제공. SOP §1 대원칙(원문 그대로)에 따라
그림은 손대지 않고 캡션에서 "데이터셋별"을 제거, `(a) Severstal, (b) AITeX` 패널 표기로
대체하였다. 본문 그림 5-5 참조에도 "(그림 5-5는 Severstal·AITeX 사례)" 명시.

## STEP 5 결과 (2026-09-16)

- docx `word/media/`에서 **44개 파일**을 `GRADUATION/figures/`로 복사·개명. 크롭·합성·재렌더링 없음(SOP §1 대원칙).
- 본문 참조 경로 44개 ↔ 실제 파일 44개 완전 일치(누락 0, 미참조 0).
- 매핑 교차검증(육안): fig3-3(GAN 흐름도), fig3-9a/b(Train/Test), fig5-1(AROMA 파이프라인),
  fig6-2(결함 하위유형 4행), fig6-4(배경 유형 5행) 모두 캡션과 일치 확인.

## 해상도 미달 / 재생성 검토 대상

- 현재까지 식별된 항목 없음. 다만 `fig3-4.jpg`(3.7KB)·`fig3-6.jpg`(3.9KB)는 원본 자체가 매우 작은
  생성 patch 이미지이므로, 인쇄 시 확대 배율에 따라 가독성 확인이 필요하다(원문 그대로 사용, 재생성 금지).
